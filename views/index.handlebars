<h1>Tacos!</h1>

<h2>Your Favorite Tacos!</h2>

<ul>
  {{#each tacos}}
    {{#unless devoured}}
      {{> tacos/taco-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Tacos that have been eaten! Nom Nom!</h2>

<ul>
  {{#each tacos}}
    {{#if devoured}}
      {{> tacos/taco-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Taco</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ta">Taco Name:</label>
    <input type="text" id="ta" name="name">
  </div>

  <div class="form-group">
    <label for="dev">Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0"> UnDevoured
  </div>

  <button type="submit">Add Taco</button>
</form>
